import 'package:flutter/material.dart';

class TelaDeTreinos extends StatelessWidget {
  const TelaDeTreinos({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Em breve!',
        style: Theme.of(context).textTheme.titleMedium,
      ),
    );
  }
}
